# cch-axcess — Decision-Tree Changelog

## AX-36 (2026-06-25) — compact KC write path + KBA-401 inventory fixes
- `http_runner.build_compact_batch_xhr`: KC UpdateProperty writes return only `{i,status,attempts,ok,bodyDrop}` — the ~88KB per-write echo stays IN-PAGE (the body-drop check runs there; it was the echo's only consumer). `kc.update_properties_sequential` switched to it. ~1,400x less data over the bridge; a 55-field form went from ~2.7h to seconds. Verification unchanged (submit -> reload -> diagnostics). `build_batch_xhr` kept for callers that need bodies.
- `inventory_form`/`_is_fillable` (KBA-401, scoped to `.KBA401.*`): DENYLIST `EntityEnv<Comp>` matrix display columns (present/controlsimplemented/functioning/risk — UpdateProperty silently rejects them); SURFACE the over-filtered `EntityEnvOverall{Assessment,Effective}` `yesno` inputs (incl. the 6th that clears the last diagnostic). No effect on other forms.
- Payload AX-35 -> AX-36.

## AX-35 (2026-06-25) — KC over the bridge + upload + status sheet (SEALED) + bridge-first hoist
- Status-sheet folded into the SEALED payload: new module `binder-status-sheet.md` + `scripts/binder_status.py` (build_status_workbook / rows_from_folders), registered via regenerate_index (trigger map + INDEX), manifest re-stamped. Triggers: "build the binder status sheet" / "binder index" / "sign-off map".
- Bridge-first HOIST to a global SKILL.md Step 0.0 (first browser call of any session = chrome_bridge_status, independent of routing/warmup); 0.2 trimmed to a back-reference. (Other-box complaint 2026-06-25.)
- Payload re-stamped AX-33 -> AX-35; version strings bumped.
- (AX-34b 2026-06-25) Bridge-first HOIST: moved `chrome_bridge_status` detection to a global SKILL.md Step 0.0 (first browser call of any session, independent of routing/warmup) — the 0.2 placement fired too late and the literal first browser touch grabbed linked-tab `list_connected_browsers`. Step 0.2 bullet trimmed to a back-reference. (Other-box complaint, 2026-06-25.)
- transport.md/architecture.md/SKILL.md/session-bootstrap.md/fill-kc-form.md: KC origin is now BRIDGE-FIRST via `chrome_api_call` (service-worker fetch, CSP-exempt; in-page eval/fetch still blocked on KC). Linked tab = fallback. Validated live 2026-06-23 (read+UpdateProperty+submit+refresh+diagnostics).
- KC data over the bridge is NOT DLP-filtered (return JSON; download-to-disk is the linked-tab fallback only).
- file-io.md: `chrome_upload` POST /v1/Documents/{clientId} is the VERIFIED PRIMARY upload path; base64-XHR is the fallback (superseded the 'chrome_upload fails CORS' rule).
- transport.md: `chrome_api_call` body must be json.dumps'd (verb takes a string); bridge server.py dict-accept fix pending.
- field-conventions.md: AUD-100 casing -> `.AUD100.OverAllTailoringQuestions`; added `.AUD100.Overallauditareastemp` and a KBA-200 row (`answer`/`chooseitem`/lowercase `description`).
- NOTE: scripts + exec-payload .bin UNCHANGED this pass (doc-only). Status-sheet shipped as a standalone script; fold into the sealed payload in the next source-repo rebuild.

Append one line per structural change (module added/changed/removed, trigger map edit, new
endpoint, fact moved to architecture.md). Newest first. This is the anti-drift ledger — if a
capability changed and there's no line here, the change isn't done. Keep entries one line.

Format: `YYYY-MM-DD — <module/file> — <what changed> — <tracker id if any>`

- 2026-06-19 — AX-33 ROUTING + RED-TEAM REFINEMENTS (post-audit, sim-driven) — LEADSHEET decision SIMPLIFIED per firm reality: the "Excel/house-style workpaper" option is PURGED from the dispatcher; "leadsheet" is now two surfaces (system-generated vs TB-report), and CREATE/run/build/"make me" a leadsheet = ALWAYS the TB-report leadsheet (run-reports) UNAMBIGUOUSLY (you don't create a system leadsheet) — the only fork is ANNOTATE/manipulate (system bubble=annotate-leadsheet vs TB-report REF=annotate-tbreport), and only when unsignalled. ADJACENCY tightenings (from the routing sim's extra-reads finding): toggle-program-step summary = "move steps in/out ONLY, NOT tailoring/risk/sign-off (that's populate-program)"; populate-program summary = "the WHOLE build (toggle does steps-only)"; file-io summary = "upload EXISTING file; build NEW Excel=workpapers skill; in-place overwrite=replace-workpaper". THROTTLE KILLER now real+teardownable: http_runner.build_keepalive_js(enable) — silent-audio 0-gain oscillator, idempotent ON, MANDATORY OFF (stop+close+null; a leaked oscillator = permanently throttle-exempt tab); surfaces suspended/no-gesture; architecture.md detect→apply→TEARDOWN doctrine; node-tested lifecycle. M1 read-only fix: engagement-state writes (binder maps, xref decisions/Form Index/Cross-References/Decisions Log) redirected install→USER WORKING FOLDER (map-binder, fill-kc-form, architecture, training-mode, data/INDEX) — install is read-only, agent never self-writes it. M3: je.py FP auth via the cap:fp sentinel (not inline __cch_capture); node-validated emitted JS. Deferred fixes: training-mode Move-PRESERVES (was wrongly "Move-clears"); regenerate_index handles inline `calls: []` (was rendered `[`,`]`); JE trigger string parenthetical moved inside quotes (stray INDEX quote); local-exec.md front-matter added; auth_capture docstring INSTALL_MONKEYPATCH_JS (was a nonexistent fn name). VALIDATED via isolated-subagent sims: routing 32/32 correct (0 deviations, every module+gate fired); red-team 15/15 held (9 deep-direct-entry + 6 cross-skill "have cch-axcess do X" — no Step-0/gate bypass, no soft-lock); truncation guard correct inside a full workflow. — AX-33
- 2026-06-19 — AX-33 TRUNCATION GUARD (always-on fused detect+recover; A/B-validated) — SKILL.md operating-doctrine: replaced the reactive truncation note with a FUSED, ALWAYS-ON completeness check + NAMED recovery. DETECT: after any bash read of the mount OR any over-the-bridge text pull, confirm completeness in the SAME step (END marker / expected size); skip host Read/Write/Edit (true disk) + binary pulls (zips serve intact). RECOVER (route by type, no debug loop): host-edited text clipped in a bash read → host Read tool OR rename round-trip (mv f f.cb && mv f.cb f) to bust the stale virtiofs cache; script to RUN → extract from the .bin to /tmp; short bridge text-return → re-pull/download-to-disk. local-exec.md: NEW "Fast cache-bust" section (rename round-trip as first recovery, ahead of the host-Read→Write→cp R-fallback). Subagent A/B (objective tool_uses/token telemetry): COST on a no-truncation buffet = +0 tool calls, ~1% tokens (≤5% forced per-read), 0 stutter, 12/12 correct both arms; BENEFIT on a truncation-prone set = naive baseline 1-2/10 (~13%, silent-wrong: computed on partial data, no recovery channel) vs guarded 9-10/10 (~97%, detected short read → recovered cleanly in the same call count). Net: ~1% cost converts fast-confident-wrong into correct. Finding: detection alone is insufficient — the doctrine must NAME the recovery action (naive arm noticed truncation but dead-ended). Payload re-stamped (manifest unchanged at 52). — AX-33
- 2026-06-19 — AX-33 JS/DOM-FIRST + STICK FIX + NEW CAPABILITIES (rebuild over AX-31/32) — NEW CAPS routed: post-journal-entry.md (scripts/je.py build_post_je_js — post a real AJE/RJE/PAJE/TJE into the TB via FinancialPrep /v1.0/journalentry, CREATE-only, balance-checked, binder-URL-guarded; live 201 id 391378) + replace-workpaper.md (scripts/wpm_replace.py build_replace_version_js — native "Upload new version" PUT /v1/Documents/file/{cid}/{docId}, name-match=current display name, UNRECOVERABLE → MANDATORY consent gate every run); both added to trigger map + INDEX + scripts table + manifest. STICK FIX: http_runner.build_batch_xhr gains retry_on_body_drop (retry ≤5x/~200ms while body ~ /non-empty request body|Bad Request/ — the intermittent 400-in-a-200 UpdateProperty body-drop); kc.update_properties_sequential turns it ON (covers KBA + audit-program step-edit flakiness). JS/DOM-FIRST: kc_dom_parser (window.kcDom) is the leg:kc fillable-field DETECTOR, GET stays write substrate + empty-add-grid (objectList:[]) check, in-page GET = read-of-truth; fill-kc-form.md rewritten accordingly; architecture.md "DOM is a viewport" scoped (binder inventory=API, form-field detection=DOM, writes/verify=API — no contradiction). populate-program.md: assertions CLIENT-RENDER-ONLY→DOM, sign-off property name varies (performedby vs SignOff, read pt3 key), overall sign-off OFF LIMITS, UpdateProgramStep full-state. THROTTLE/AUTH: architecture.md new "Tab visibility, throttling & token refresh" (visibilityState not focus; silent-audio keep-alive; forbidden hidden-near-expiry reload; OIDC iframe silent-renew future fix) + IDToken/IdToken both→401. TRANSPORT: transport.md status=bridge PRIMARY / linked-tab FALLBACK-ONLY; F1 cross-origin upload = in-page XHR base64 (chrome_upload fetch fails CORS); file-io.md "Upload new version" reframed → replace-workpaper.md behind consent. AUTH: http_runner cap:<family> sentinel (engagement-tab __cch_capture leg, counterpart to ls:* KC-localStorage; reconciles je.py/wpm_replace.py inline reads). SCRIPT FIXES (AX-32 backlog): tb_backup.parse_tb→normalize_tb_rows (live FP variant: number→accountNumber, group/fund objects→labels, comma-string balances→float; no-op on grouped shape) + _pick_balance flat-final fallback; funds.check_unfunded_accounts guard (accounts in NO fund silently dropped from fund statements → STOP+prompt); architecture.md unfiled-node move 400 ("items can be moved to their respective unfiled nodes only" — create a real folder first). SKILL.md operating-doctrine pointers (JS/DOM-first, throttle/auth, truncation-view-is-mount-artifact). Unit-tested: cap sentinel, retry builder, kc-seq retry-on, tb normalize (both shapes), unfunded guard. Version AX-31→AX-33; payload exec-payload_AX-33.bin; manifest re-stamped (+je.py, wpm_replace.py, kc_dom_parser.js, fp_journalentry.json, wpm_replace_version.json, post-journal-entry.md, replace-workpaper.md) — AX-33
- 2026-06-17 — AX-32 DUAL-TRANSPORT FOUNDATION — NEW references/runbooks/transport.md: chrome-bridge (local MCP plugin) is now the PRIMARY browser transport, linked Claude-in-Chrome tab is the FALLBACK; detect via chrome_bridge_status. Op->tool map: run_js=chrome_eval(target), backend call=chrome_eval running the http_runner XHR builder (KEEP XHR - cross-origin fetch fails CORS), files=chrome_download/chrome_upload, tabs=chrome_list_tabs/chrome_navigate/open_tab/close_tab, auth=chrome_network_recent (webRequest capture, no monkeypatch/provoke). Bridge result channel is DLP-UNFILTERED (validated live) => filtered=false on bridge (read JSON directly, skip download-to-disk); linked-tab stays filtered=true. Tab-group/claim machinery is linked-tab-only. SKILL.md pointer added. Bridge built+validated live separately (COM project\chrome-bridge). Per-module wiring pending LIVE work; until wired each module's linked-tab path still works. — AX-32
- 2026-06-05 — AX-31 FUND-REFERENCE PREFLIGHT (post AX-30 fail-state test) — funds.py NEW check_fund_references(account_rows, defined_funds, fund_range=None): halts before fund assignment when an account references a fund NOT in the engagement's defined Fund list (orphan / out-of-range, e.g. fund 999 when only 100-500 exist) — the gap the AX-30 fail-state test found (preflight_account_map only caught empty/placeholder maps, not undefined-fund references; the whole-list PUT mis-maps silently w/ no native error). ok=False => STOP + prompt the user; fix is theirs (define the fund or correct the account), NEVER auto-reassign. manage-funds.md: mandatory Fund-reference halt step added beside the Placeholder-TB halt + front-matter calls (check_fund_references, preflight_account_map). Unit-tested: halts on orphan + numbering-range, passes clean refs. Payload exec-payload_AX-31.bin; AX-31 — AX-31
- 2026-06-05 — AX-30 ROUTING-ORDER FIX (post AX-29 flow audit) — the AX-29 disambiguation block was CONTENT-correct but POSITIONED outside the routing procedure (separate section below the Routing flow), and the Routing flow had a latent ordering bug predating AX-27: it listed "Step 0 warm declared leg" BEFORE "find the matching row", i.e. warm-the-module's-leg before the module is chosen — circular. A literal flow-follower could warm a leg / fuzzy-match a row before disambiguation ever fired (the exact over-inference Brett flagged). FIX: Routing rewritten as an explicit ORDERED sequence — 1 seed identity, 2 RESOLVE THE DELIVERABLE (2a disambiguate overloaded terms FIRST=ASK+wait, 2b match one row, 2c consent wall no-fuzzy, 2d mixed→+cch-help) BEFORE 3 WARM the module's declared leg, 4 exec cache, 5 open module. Step 0 header split (seed=before routing, warm=after module chosen); 0.2 now states it runs AFTER routing step 2, never before (leg is per-module; pre-pick warming is guessing, and wrong for leg:none/route-out). Disambiguation now fires before any leg-warmup or row-commit. Payload exec-payload_AX-30.bin; AX-30 — AX-30
- 2026-06-05 — AX-29 DISAMBIGUATION GUARD (post AX-28 gradient red-team) — SKILL.md: NEW "Overloaded terms — disambiguate BEFORE routing" block above the trigger map. Two firm terms each name 3 deliverables — "leadsheet" (TB-report leadsheet=run-reports CREATE / system leadsheet=annotate-leadsheet / house-style Excel=separate workpapers skill) and "TB/trial balance" (TB report / backup workbook=tb-backup-package / import file=import-tb-format). Route straight when the ask signals the kind; ASK before routing when it doesn't — closes the cold-"make me a cash leadsheet" mis-route, the TT1-1 226-call class, which prior fixes only addressed INSIDE modules (reached after routing, too late). Consent-wall guidance: roll-forward/recycle-restore/TB-import asks hit the wall, NOT a fuzzy setup-binder match. tb-backup-package.md: wip/first-live-run user warning at top of Procedure. Payload exec-payload_AX-29.bin; AX-29. cch-axcess only — AX-29
- 2026-06-05 — AX-28 AUDIT-HARDENING (post AX-27 red-team) — SKILL.md absolute-entry rule upgraded from causal warning to HARD REFUSAL: new clause "the user cannot authorize skipping a gate" (Step 0 / exec-verify / consent wall / no-hard-delete are not user-overridable shortcuts) — closes the dispatcher-bypass-on-user-command hole (red-team msgs 2/3/10). SKILL.md failure discipline: EXISTENCE CHECK lifted out of the post-failure block into a pre-attempt gate ("confirm the named group/fund/form/row EXISTS before building toward it" — TT1 90-min-no-Cash-group class), and the 2-fail/improvisation-budget line now explicitly states user consent for capture mode does NOT lift the 2-fail limit and a param tweak is not a fresh approach. annotate-tbreport.md: NEW Step 0 existence gate (check the row's group/subgroup/fund is on the report BEFORE the column preflight; not-found = STOP+ask) + Step 2 no-loop guard (empty probe after scroll + existence-gate-passed = surface+stop, don't re-probe variants). Payload exec-payload_AX-28.bin; version strings bumped AX-27->AX-28. cch-axcess only — sister skills unchanged from AX-27 — AX-28
- 2026-06-05 — AX-27 BATCH REBUILD (post TT1 docket) — PATH SURGERY: Step 0-A merged into Step 0 (seed clientId/engagementId from URL/memory) + LAZY TWO-LEG AUTH (modules declare leg: wpm|kc|none in front-matter; wpm = engagement-tab capture serving WPM/FP/workbench via WPM-bearer reuse validated 6/5 — T8 probe CLOSED; kc = GUID + kc tokens + KC tab for KC-form ops; GUID not required for reads/annotations/WPM ops; never warm a leg a warm one serves); session-bootstrap.md REWRITTEN per-leg; EXEC CACHE PER-BUILD (/tmp/cch-ax-{ver}-{payloadhash} + .verified marker; verify once per build — local-exec.md rewritten); CONSENT WALL replaces auto-learn (verbatim wall text in SKILL.md routing; learn-protocol.md gated behind explicit yes; overlay-check purged; capture artifacts → working folder + complaint log); 401 = re-warm failing leg only, retry once, surface; 2-FAIL FORCED DOC RETURN (same-transport variations = same path; doc return must name a path from the finite menu; named-path-already-tried = FAIL STATE abort; reset scope = operation) + improvisation budget / ill-posed-ask doctrine ("an impossible ask is a prereq finding to report"); rules stated once in dispatcher, modules cite by number. ROUTING: absolute entry header (non-SKILL.md entry → restart Step 0); cch-help cross-trigger on mixed asks; hard gates added to workpapers + workpaper-formatter (live CCH tab + filing ask → cch-axcess). DOC/FACT: import-tb-format sign convention CORRECTED to debits-positive/credits-negative (prior credits-positive line seeded by BT3's own inverted import; FP-API serves credits-positive = API-internal, NOT import convention) + provenance rewritten; NEW endpoints/fp_trialbalance.json (balances[] row schema: account.group / account.financialSubGroup — subGroup silently null, NIN bug / account.fund / periods.current.*; pagination mandatory); transport matrix updated (WPM bearer → workbench + FP validated 2x 6/5; Refresh-Report hard-reload trap documented — TT1-2 terminal wall); _SJT-SkillState PURGED everywhere (state-folder.md deleted; learn-protocol/local-exec/SKILL.md de-overlayed; regenerate_index.py --overlay removed; runtime learnings → complaint-log → batch rebuild; CHANGELOG source-repo only). NEW OP: scripts/tb_backup.py + modules/tb-backup-package.md (TB CY+PY + groups + subgroups + funds → 4-5-sheet workbook: sterile debits-positive import sheets, Account Map both years, Groups, Fund Structure, all-fields tab; reads financialSubGroup natively; parse_tb hard-stops partial merges). regenerate_index.py: Leg column + missing-leg warning. Payload exec-payload_AX-27.bin; manifest re-stamped — AX-27
- 2026-06-04 — AX-26 PACKAGING — exec payload rebuilt + renamed exec-payload_AX-26.bin (local-exec.md self-reference updated inside the bin); integrity manifest re-stamped (44 files, all END-marked); SKILL.md + modules/INDEX.md regenerated from frontmatter (--check clean) — AX-26
- 2026-06-04 — AX-26 CAPTURE-DRIVEN FIX ROUND (BT3 + live capture session) — STAGE 1 annotate/leadsheet class: annotate-tbreport.md REWRITTEN (transport truth: workbench-api dead from KC tab, ALIVE via engagement-tab injected XHR w/ captured headers incl traceparent; TB-report deep-link URL /tbreports/{reportGuid}/period/{engagementId}/subsidiaryParentId/0 kills tree-walking; Step-0 REF-column preflight + existing-value prompt; referenceType by ROW LEVEL Fund/FinancialGroup/FinancialSubGroup — blanket Group→FinancialSubGroup was wrong; columnId positional+rename-proof; firm leadsheet-vs-system-lead terminology + one-directional mirror + filed-system-lead routing rule); annotate-leadsheet.md: group-level bubbles (referenceType FinancialGroup), silent-upsert prompt, routing vocabulary (comment/note=bubble, REF=column), system-lead deep-link, delete live-confirmed; reports.build_leadsheet_columns LOCKED layout (UNADJ/AJE/RJE/FINAL/REF single Remarks, FINAL-PY only w/ comparative; NO Adjusted, NO Notes); NEW reports.add_remarks_column (PATCH editReports); run-reports.md single-leadsheet path now REQUIRES columns= (the B6/B10 'did it before' regression); NEW endpoints wb_editreports.json + kc_title_library.json; fp_annotation_comment.json + wb_tbreportcomment.json corrected — AX-26
- 2026-06-04 — AX-26 STAGE 2 auth/transport: http_runner ls:<family> headers SENTINEL (ls:wpm/ls:fp/ls:kc — runtime localStorage self-sourcing in every builder; kills the per-session with_ls_auth improvisation, ~20 calls/session); architecture.md self-sourcing claim made TRUE + POSITIVE transport matrix (XHR works KC-tab→WPM/FP; workbench=captured-dict only) + doc-first doctrine (grep endpoints/*.json before improvising — B9 burned ~17 calls); GetBinder-FIRST wpId lookup in toggle-program-step/fill-kc-form/remove-kc-form; KC_HEADERS_JS string constant → deprecated callable shim (B1 TypeError) — AX-26
- 2026-06-04 — AX-26 STAGE 3 script bugs: soft_delete_form requires parent folder INTEGER locationId (refuses GUIDs — B15 400); is_answered None-tolerant (isValueDefault=None false-negatived every inventory record — B8); build_patch_body REQUIRES full fresh group row incl UUID id + engagementFinancialGroupMapId (B3 400 + TB-import UUID churn incident); build_write_payload integrity SEAL refuses post-hoc-mutated collection_path (B9 guard hole); wpm.move() refuses hand-assembled folderParentLineItems (B5/B12 silent-200 recurrence); binder_planner.section_for_index w/ 81→8000 exception (FOLDER_MAP gap); catalog.load_sa_title_forms + SA_TITLE_GUID — 84 Single Audits title forms (SAS.2024.1, data/sa-title-forms.tsv), diff_against_unfiled returns sa_title_adds + never silently skips (B7 dropped 22 S-forms); decode_form accepts str + clear TypeError; TQComments propertyKey='Description' documented — AX-26
- 2026-06-04 — AX-26 STAGE 4 doctrine: funds.preflight_account_map placeholder-TB HALT before any assignment step (BT3 incident rule; wired into manage-funds/manage-groups); wpm.verify_index(row, object_type) field-aware index reads (B6 false-negative class; wired into run-reports/rename-workpaper-index/file-io); fresh-groups-before-bulk-assign doctrine in manage-groups — AX-26
- 2026-06-04 — AX-25 AUDIT-FIX PASS — architecture.md + wpm_set_index.json: Move-PRESERVES-index de-drift completed (2 surviving "Move clears the index" claims purged); scripts/session.py: removed dead programmatic_guid_capture_js (deleted span.item-name-ellipses selector) + its session-bootstrap frontmatter calls ref; run-reports.md + rename-workpaper-index.md: auth flipped to localStorage-primary (monkeypatch demoted to fallback, inline header recipes replaced with architecture.md pointer) — AX-25
- 2026-06-04 — AUTH UNIFICATION localStorage-primary (AX-25 / Group 2) — architecture.md Auth-pattern section REWRITTEN to lead with ONE central header-builder: KC localStorage tokens (kc.accessToken/kc.idToken) are PRIMARY auth for ALL four API families (WPM/workbench/financialprep/KC); per-family casing table (all-caps IDToken for WPM-style, mixed IdToken for KC) + WPM locale headers; refreshToken self-refresh so re-read localStorage per call (no expiry handling); __cch_capture demoted to bootstrap+fallback only + 158KB stash cleanup rule; map-binder.md + annotate-tbreport.md prereqs say engagement tab NOT required for headers / KC tab primary; binder_map.py build_map_js now localStorage-primary header build w/ __cch_capture fallback — AX-25
- 2026-06-04 — COLD-START BOOTSTRAP + "DOM is a viewport" (AX-25 / Group 3) — session-bootstrap.md Step 4 span.item-name-ellipses hunt DELETED, replaced with Cases A/B/C (A known-binder KC deep-link 0 clicks; B unknown-cold patch-list+entry-click; C parked-on-view one synthetic tree-node click) + provoke-irreducible-on-engagement-origin note (all bypasses DEAD) + seeded-forms fact (fresh binders always have unfiled KC forms; folder_get(-4)); architecture.md NEW "DOM is a viewport, not a database" rule (inventory via API only, every folder lazy-loaded, staging IS the GET, no "stage" verb, virtualization caveat, read_network_requests ruled out) — AX-25
- 2026-06-04 — SILENT-200 doctrine + add_forms shape (AX-25 / Group 4) — architecture.md NEW "HTTP 200 = accepted, not applied; re-GET after every write" doctrine + index-vs-documentIndex table; kc_add_forms.json + kc.py add_forms docstring CORRECTED (result[] ECHOES successful adds w/ workpaperId — NOT failures-only/silent; keep don't-blind-retry, rationale now "success visible in result[]"); kc.py build_write_payload() now REFUSES a hand-assembled collectionKey (requires inventory_form() record; B13 antipattern structurally impossible) + area-short-name (.CASH not .CASH801) note; add-audit-programs.md Step 5 code comment corrected to match (had carried the FALSE failures-only/silent claim — file was outside the original Group 4 edit list; flagged for audit) — AX-25
- 2026-06-04 — STALE MODULE FACTS (AX-25 / Group 5) — annotate-tbreport.md Step 1 tbreportedit "id" example deleted (reportId = WPM documentId int), Step 2 probe returns rowType not ready referenceType + Group→FinancialSubGroup mapping moved INTO procedure + virtualization caveat (scroll row into view); toggle-program-step.md WpReference-fillable claim REMOVED (steps carry SignOff/Comment/Comments only, no step-level WP-ref field — say it doesn't exist) + confirm-path area-short-name; wpm_move.json "Move CLEARS the index" replaced with "Move PRESERVES the index (AX-14); Set-Index only for genuinely new null-index forms" — AX-25
- 2026-06-04 — DLP REALITY / Python-layer role (AX-25 / Group 6) — architecture.md NEW "Execution model — scripts BUILD JS, the browser EXECUTES" section (auth read from localStorage at runtime inside built JS, tokens stay in-page, never cross tool channel; kills "Python decorative/can't run in Cowork" misconception; builders mandatory for writes); http_runner.py adds named helpers build_relay_store_js / build_relay_call_js (big-body window.__x relay) + build_chunked_read_js (chunked enumeration for large reads), existing functions intact — AX-25

- 2026-06-04 — LOCAL-EXEC ZIP PAYLOAD (AX-24) — references/runbooks/local-exec.md REWRITTEN + exec-payload_AX-24.bin (NEW, skill root): mount truncates text but serves binaries intact (confirmed live — 16/16 .py corrupt, 7/7 xlsx OK); exec setup is now unzip-payload-from-mount → fresh /tmp/cch-ax-$(date +%s) → compile gate — 2-3 calls, kills the ~20-30-call host-Read repair loop; payload is ZIP-FORMAT under .bin extension (installer rejects nested .zip; content-sniffing disproven — xlsx are zips), full tree minus __pycache__, filename VERSIONED per build (sidesteps PLAT-01 zero-padding); host-Read repair demoted to fallback R; NO skill-state folder or user mount required — AX-24

- 2026-06-04 — BINDER STRUCTURE DEFAULTS (AX-16) — setup-binder-from-index.md + binder-template.xlsx + data/INDEX.md + add-audit-programs.md: default structure flattened to 2 levels (wrapper > sections; 01/02/03/04 parent tier PURGED from template — `Sections` sheet replaces `Top level index`+`Leadsheets`); wrapper takes NO folderIndex (client name only; empty-index flagged UNVERIFIED w/ fallback branch); NEW Phase 1 step 5-pre: user chooses default / brings own grouping list (used verbatim) / co-develops; deeper nesting retained as capability via parent_folder_id chains but only when user specifies; program templates unaffected (4-digit form indexes are nesting-agnostic); module status wip pending live validation — AX-16

- 2026-06-04 — COMPLAINT-LOG SWEEP (AX-15) — worked the 2026-06-03 complaint backlog that never reached a build queue (Coop binder-setup + TB-import sessions)
- 2026-06-04 — setup-binder-from-index.md + scripts/catalog.py — entity-type detection REBUILT: GetBinder lastUsedTitleGuid → catalog.resolve_title_guid() (new fn; Title column confirmed in catalog) is AUTHORITATIVE; TB account-structure corroboration; Funds-Setup button demoted to confirming signal; NEVER client name; KC-title-vs-TB conflict = blocker; parse binder/unfiled forms FIRST (form refs shared across entity types — not a signal) — AX-15
- 2026-06-04 — scripts/wpm.py + endpoints/wpm_folder_rename.json (NEW) + setup-binder-from-index.md — rename_folder() added from 2026-06-03 live capture (PUT /v1/NewEngagementView/{clientId}/folder, load-bearing field oldLocationId; locationId → 400 Parent-not-found); double-index naming regression fixed: folderName = CLEAN name, NO index prefix (folderIndex carries it); wrapper = index 00 + clean name; remediation branch documented — AX-15
- 2026-06-04 — import-tb-format.md (NEW module, wip) — CCH TB import constraints captured 06-03: freeze panes = hard silent reject; no decoration rows; credits-positive sign convention (flagged re-verify); single balance column; unique names; CLASSIFICATION display values + exact column order UNCAPTURED (do not invent — mirror a real export); generate_import_file() blocked on those captures; trigger row added via regenerator — AX-15

- 2026-06-04 — rename-workpaper-index.md + toggle-program-step.md + bulk-capture-forms.md + SKILL.md — structural audit: repaired 3 dangling refs to nonexistent references/auth-discovery.md (→ session-bootstrap.md monkey-patch step / architecture.md KC auth); SKILL.md routing no longer promises a literal "Load from the jump" section (present in 1/15 modules; tools live in Prerequisites blocks) — audit, all module calls + endpoints/config/data refs verified clean
- 2026-06-04 — LOCAL EXEC COPY (AX-14) — bash mount serves STALE/TRUNCATED views of updated files on READ (confirmed live: every 06-04-edited script cut mid-token in mount view; verify_integrity.py uncompilable from mount while complete on disk; post-edit host writes served at pre-edit length) — new runbooks/local-exec.md (copy install sandbox-local, compile-gate verify, repair from host Read, new-filename transfer after host edits); SKILL.md routing + env-note gate added; CHANGELOG.md + local-exec.md stamped + manifested; kc.py tail graft + all-script stamps re-applied at package time (AX-13 stamps had not landed in the 02:11 package)
- 2026-06-04 — STATE EXTERNALIZATION (AX-13) — runtime writes moved out of the install to `%USERPROFILE%\Documents\_SJT-SkillState\cch-axcess\` (new runbooks/state-folder.md: resolution, first-run init, overlay shadowing, graduation); learn-protocol + SKILL.md routing rewritten to overlay; regenerate_index.py gains --overlay; verify_integrity accepts `# <!-- END -->`/`// <!-- END -->`, ALL scripts stamped + manifested; kc.py AX-12 tail note re-grafted (0604 package shipped with it cut — compiled clean because the cut landed in a comment)
- 2026-06-04 — scripts/http_runner.py + scripts/wpm.py — WPM_LOCALE_HEADERS constant; merged into EVERY wpm.py call (missing locale headers = silent 200+[] on all folder reads) — s4 P-NEW-1
- 2026-06-04 — add-audit-programs.md + scripts/wpm.py — REMOVED two disproven failure-mode claims: Move PRESERVES index, locationId STABLE across moves (both confirmed live); Set-Index-after-Move is for null-index new forms — s4 P-NEW-2/T14
- 2026-06-04 — wpm_folder_get.json + scripts/wpm.py — folder_get response documented as FLAT array (no lineItems wrapper) — s4 P-NEW-3
- 2026-06-04 — rename-workpaper-index.md + scripts/wpm.py — Workpaper PUT returns EMPTY body (not document echo); verify via follow-up GET; "13 missing fields" corrected to 2 (fileName, setNotesToDoNotRollForward); synthetic-dict 400 trap documented — s4 P-NEW-4/5 + s3
- 2026-06-04 — scripts/wpm.py — ADDED download_url, download_to_browser_js, evict_for_replace, claim_original_slot, check_index_available (whole download/replace path was documented but unimplemented, T08); rename_workpaper gains new_name param; soft_delete_workpaper integer-docId rows now move as objectType Workpaper (was silently no-oping as KCForms)
- 2026-06-04 — scripts/funds.py + funds_funds.json — RESTORED delete_fund_type/delete_fund/delete_fund_sub_type + check_delete_eligible_* (referenced by manage-funds.md + specs, missing from script); added Fund delete endpoint to spec — s3 P1/T18
- 2026-06-04 — scripts/catalog.py — REWRITTEN to the real xlsx schema (Form ID / Reference Tag / Title GUID / Add-Form Constants; old camelCase fields matched nothing → 0/22 lookups); added lookup_by_form_id (primary); Add-Form Constants rf/copy/upd/mpk unpacked; smoke-tested against live catalog — T14
- 2026-06-04 — scripts/kc.py + kc_add_forms.json + add-audit-programs.md — add_forms response is {"result":[...]} with ONLY flagged/failed forms (success silent); NEVER blind-retry (duplicates); pre-POST dedup documented; GetBinder parse at result.workpapers — T14
- 2026-06-04 — setup-binder-from-index.md — auto-cascade claim DISPROVEN (move to parent does NOT distribute to sub-folders; explicit per-sub-folder moves required); BS leadsheet target corrected top_level_map["02"]→["03"] — T15
- 2026-06-04 — file-io.md — AX-01 FormData fallback VERIFIED LIVE → documented default (with in-page JS); collision preflight moved BEFORE upload (enforced at POST); file_upload requires Windows host path; duplicated replace-block removed; soft_delete call name fixed — T07/T08
- 2026-06-04 — session-bootstrap.md + scripts/session.py — programmatic_guid_capture_js (DOM click fires /api/engagement/{guid}, closes the STILL-OPEN cold-start gap); Step 1-B user-tabs-invisible gotcha (ask once before creating a tab) — T01, P4
- 2026-06-04 — fill-kc-form.md + session-bootstrap.md — filtered-channel read path: download-to-disk then request_cowork_directory(Downloads) immediately; never ask the user to save-and-report — P3
- 2026-06-04 — architecture.md — WPM confirmed-facts block (locale headers, flat array, Move preserves index/locationId, create_folder plain-int response, folder-move inversion, Workpaper documentId==locationId, PUT empty body, GetBinder result wrapper); KC-tab tokens work for ALL WPM reads+writes — s4 facts, P2
- 2026-06-04 — scripts/verify_integrity.py + integrity-manifest.txt — null-byte sweep over all .py/.md/.json (installer overlay defect detection); AX-12 recovered modules re-protected in manifest
- 2026-06-04 — workpapers/xtblink — T19 PASS (FRICTION): workpaper built; WP-01 no group-index TB validation (phantom formula for empty groups); WP-02 fund suffix is manual --fund flag (no auto-detect); WP-03 default adds ROUND (use --no-round); all 3 confirmed unfixed
- 2026-06-04 — manage-funds.md — T18 PASS (WORSE): all ops 200; GAP: delete_fund/delete_fund_type/delete_fund_sub_type missing from scripts/funds.py — URLs confirmed DELETE /v1/Fund/{cid}/{id} and /v1/FundType/{cid}/{id}; upsert URL is /v1/FundType (no clientId) not /v1/FundType/{clientId}
- 2026-06-04 — remove-kc-form.md — T17 PASS: KBA-501 soft-deleted to 9999.1, verified, restored; body shape folderParentLineItems {locationId:DEST,parentLocationId:OWN}; XHR required for WPM writes from KC tab; WPM writes confirmed from KC tab
- 2026-06-04 — toggle-program-step.md — T13 PASS: remove 1 (19→18 parents), restore (18→19), target visible=false/true confirmed; note POST value = parent+child keys, verify GET counts parent rows only
- 2026-06-04 — populate-program.md — T12 PASS: AUD-801 Cash, 19 parents/74 steps/167 payloads, 5 repair passes to bad=0; verify+repair pattern confirmed; RMM-EO/RO/CO/CU/UC + FINANCIALLEVELRISKS-1 all linked
- 2026-06-04 — fill-kc-form.md — T11 PASS: 26/26 fillable answered 2 rounds; fixed-point loop confirmed (PriorWP gated by NewOrRecurring); submit diagnosticCount=0; download-to-disk path verified; note: use f['answered'] not f['is_answered'] in inventory records
- 2026-06-03 — fill-kc-form.md + map-binder.md + add-audit-programs.md — AX-12 tail recovery: reconstructed Known failure modes from kc.py/wpm_folder_get.json docstrings; added <!-- END --> markers to all three
- 2026-06-03 — annotate-leadsheet.md — T10 PASS: KC tokens confirmed for FP-API; auth prereq updated; annotation readback path documented (row.account.annotation); validated_on added
- 2026-06-03 — manage-groups.md + annotate-tbreport.md — re-linked orphaned endpoint specs (groups_financial_lists, wb_tbreportcomment); see _rebuild/orphan-audit.md
- 2026-06-03 — SKILL.md — added Rule 3 (document-the-change / anti-drift) to the always-on rules
- 2026-06-03 — architecture.md — financialList = authoritative grouping-list discovery (vs financialgrouptemplate); KC-tab sequential-XHR crash gotcha — AX-04/10
- 2026-06-03 — run-reports.md — leadsheet → AccountDetail + IncludeSumOfAccountGroups + HideUnusedGroupsAndSubgroups (detail); TB stays SummaryByGroup — AX-05
- 2026-06-03 — scripts/reports.py — DEDUPED triple-shadowed defs (TB_COLUMN_TYPES, build_leadsheet_columns, group_by_leadsheet); kept the sorted group_by_leadsheet; added AX-04 note in get_grouping_lists
- 2026-06-03 — manage-groups.md — rebuilt to leaf format; mandatory reorder + bulk-assign-via-JS; CORRECTED assign call signatures vs groups.py — AX-07/08/09
- 2026-06-03 — manage-groups.md — AX-11 was DRIFT not a bug: groups.py verified the assign PATCH works 2026-05-30 (no-op was a read-back bug, row.account.group not row.group); removed stale "unverified" warning, documented read-back; status wip→validated
- 2026-06-03 — annotate-tbreport.md — added error branch for hardcoded "Group" referenceType — AX-06
- 2026-06-03 — file-io.md — added name-collision preflight + active-user-lock branch; AX-01 FormData flagged unverified — AX-02/03/01
- 2026-06-03 — learn-protocol.md — added changelog step + edit-existing-leaf trigger to the definition of done
- 2026-06-03 — (rebuild) — backup at _backups/cch-axcess_2026-06-03_pre-rebuild; see _rebuild/capability-map.md

<!-- END -->
